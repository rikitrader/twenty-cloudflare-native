# Implementation status agreement

The authoritative checklist is `docs/FULL-PARITY-TODO.md`; operational evidence
is under `docs/evidence/`. Production topology is defined only by
`wrangler.jsonc` and `src/index-native.ts`.
