# Twenty Cloudflare project instructions

Before changing, reviewing, deploying, or operating this project, read
`docs/PROJECT-MEMORY.md`.

Treat its "Hard requirements" as standing user instructions unless the user
explicitly changes them later. Update the memory when a deployment, gate,
topology, or architectural decision materially changes.
